# Shared LokServices Supabase project (auth, alpha waitlist, feedback).
# The anon/publishable key is safe to expose client-side -- Row Level Security
# on the underlying tables is what actually gates access.
VITE_SUPABASE_URL=https://jfavkudihasswkhkouxq.supabase.co
VITE_SUPABASE_ANON_KEY=
