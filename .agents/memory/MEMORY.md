- [GitHub collaboration bridge](github-collaboration.md) — await the GitHub connection record before obtaining its Octokit client.
- [Development unlock mode](dev-mode-unlocks.md) — development-only all-unlocks toggle preserves normal progression when disabled.
- [616 Survivor supplied art](survivor-616-art-assets.md) — the user's character sheets have baked-in labels; render sprite rigs for portraits, never the raw sheet.
- [Endless mode engine](endless-mode-engine.md) — unbounded world uses chunk-based obstacles rebuilt when player crosses chunk boundaries; dungeons are bounded rooms centred on the player's world-space entry point.
- [City navigation grammar](city-navigation-grammar.md) — river rows use deterministic bridge columns, while landmark metadata drives shared canvas, minimap, and HUD cues.
- [Browser regression tests](browser-regression-tests.md) — Playwright tests use an explicit Chromium executable override when the Nix shell lacks bundled browser libraries.
- [Impact physics contract](impact-physics.md) — authored force stays separate from damage, and lethal bursts must reuse the single kill/reward path.
- [Pothole impact contract](potholes.md) — pits activate only from explicit stomp/ground-shock tags and remain separate from solid collision props.
- [District setpiece contract](district-setpieces.md) — landmark incursions are optional seeded interruptions with shared simulation, bounded one-time rewards, and normal run recovery.
- [Ambiance contract](sky-ambiance.md) — clouds/shadows, birds, fireflies, litter, steam and lightning are pure render-layer decoration, while civilian/cat actors are simulated but combat-inert on their own rng stream; ground-level effects must clip to the arena bounds or `drawArenaEdges` silently paints over them.
- [Music reactivity contract](music-reactivity.md) — one beat bus arbitrates detected vs authored tempo; reactions are records, and visual ones never touch the simulation.
- [Tilt steering](tilt-steering.md) — map tilt through the gravity vector rotated by screen angle, never raw Euler axes; the two silent failures are a non-secure origin and ungranted permission.
- [Studio engine contract](studio-engine.md) — one `AudioContext` per app, one music path from studio through soundtrack to game; plain-data project model reconciled into a Tone graph whose inserts are never hard-wired, so plugins drop in; plugins stay opt-in because they run off-device code.
- [Soundtrack import](soundtrack-import.md) — dropped video files play immediately with no transcode step; "Convert to MP3" and link import both stay client-side, and link import is a direct-file fetch, never a streaming-service downloader.
- [Oddity arenas](oddity-arenas.md) — attack-block obstacles, ambient random item drops, the `ringer` circling behavior, and the `wraith` invisible-then-reveal-sway-teleport behavior; "infinite HP" enemies must stay finite or the health-bar ratio becomes NaN.
- [Run presentation layer](run-presentation.md) — value-tiered XP gems, `EnemyDef.sizeClass` as a render-only scale hook, `arachnidRig`/`serpentRig` built by reusing existing limb `PartKey`s instead of adding new ones; hazard-kind weapons place `count` nodes in a linked ring, `bonusVsStatusId`/`bonusVsStatusMult` is a reusable elemental-combo hook, `'meteor'` telegraphs then strikes from off-screen, `CharacterDef.stormCloud` is a draggable auto-cycling companion (new `PointerMode: 'cloud'`) that a follow-up pass gave manual mode control (`setStormCloudMode`, permanently overriding auto-cycle), a fourth `frost-rain` mode, and ground painting on the pre-existing `FluidTile` system (`fire-storm`/`acid-storm`/`frost` stains that outlive the cloud and refresh on repaint, washable by `water`); and: grep the roster for a theme or archetype's keywords before authoring new "themed" content, or you'll duplicate an existing palette/status-effect/lore niche.
- [Content-authoring system](content-authoring-system.md) — `data/authoring.ts`'s `palette()`/`squadWave()` and the `data/factions.ts` roster registry replace a stale CLAUDE.md claim and free-text faction strings; the `test` script now globs `*.test.ts` instead of a hardcoded file list that let new tests silently never run.
- [RTS mechanics roadmap](rts-mechanics-roadmap.md) — the reusable pieces Zero Day's freeze-select-throw work left behind (PointerMode extension pattern, world-space box selection, DOM marquee overlay, enemy-state-as-object conversion) and brainstormed future directions (rally points, control groups, zone designation, formation movement, queued commands, a capture-instead-of-throw variant) for any future RTS-shaped character ability.
- [Zero Day freeze-throw](zero-day-freeze-throw.md) — a new self-contained engine subsystem (not a `DashSkillDef`/`UltimateDef.effect` fit) for freezing enemies into inert "stone", RTS drag-selecting them, and throwing the group as projectiles; records why `DashSkillDef`/`UltimateDef` didn't fit, the `frozenUntil`/`invisibleUntil` dual-guard trick used to hide a "carried" enemy while its projectile flies, and a cooldown-sentinel bug (`lastCastAt: 0` reading as "on cooldown" at `w.now === 0`) the test suite caught.
- [Null Sector](null-sector.md) — a deliberately off-theme world (cold cyan/magenta data-center basement vs. the rest of 616's warm street palette): the one new `server-rack` `ObstacleDef` kind and its full cross-cutting checklist, `escalatingWaves()` for the 10-minute spawn-escalation rhythm, and the first real in-game use of `squadWave()`.
- [2x world extreme mode](2x-world-extreme-mode.md) — doubled-map/doubled-spawn area tier and its four dedicated characters, plus the technical constraints (`threat` union, `ImpactIntensity` range, ultimate `effect` shape) hit while building it.
- [Crew feature](crew-feature.md) — `RESCUE_ROUTE_BY_AREA` only needs an entry for areas granting more than one ally across replays (single-ally areas use just `AreaDef.rescueAllyId`); every `HubRoomDef` needs both a resident ally and activities scoped to that `roomId` or `rollCrewActivities()` silently has nothing to offer; `AllyDef.rigHint` maps an ally onto an existing `humanoidRig()` silhouette option instead of hardcoding rig choices by id.
- [Hideout ambiance and character-tier roadmap](hideout-ambiance.md) — the hideout's CSS weather (separate system from in-run `sky-ambiance.md`) needs a fixed `background-size`, never a percentage, or the animation reads as frozen on wide viewports; `heat`/`clear` room weather now animate too and the whole layer is toggleable (`meta.hideoutWeatherEnabled`, default on); also records (still unbuilt) design intent for an Artisan Valor Prime tier and an "Artisan Valor Shop" variant.
- [Achievements/unlockables page plan](achievements-unlockables-plan.md) — (unbuilt) design only: an `AchievementDef` should be a pure function over `MetaState` like `UnlockRule` already is, never a stored boolean, with a small `claimedAchievementIds` set only for one-time reward payouts.
- [Achievements page](achievements-page.md) — the plan above, built: `data/achievements.ts` + a tab inside `ArchivePanel.tsx`; reward kind is `cred`/`lootTokens` only for now, cosmetic rewards are a follow-up.
- [Run modifiers](run-modifiers.md) — `RunModifiers` picked on the Roster screen thread into `createWorld`'s `setup.modifiers`; endless mode's difficulty caps (`hpMult` ≤ 1.7, spawn ≤ 3.2/s) must have `modifierHpMult`/`modifierSpawnMult` composed *inside* them, never applied after; HordeSpin's periodic wheel event follows the `StormCloud`/`nextTickAt` timer pattern.
- [Legendary roster update — 2026-09-09](legendary-roster-2026-09-09.md) — ten playable procedural-rig legends and four distinct-size legendary LokPets; silhouettes, named mechanics, palettes, and Clockwork Beetle's rotating/accelerating clock face are design contracts and must not be silently replaced.
- [Odd Routes — 2026-09-10](odd-routes-2026-09-10.md) — four large weird arenas built from existing layout primitives, plus a persisted optional 4× spawn modifier that overrides 2× spawn pressure instead of accidentally stacking to 8×.
- [Unleashed 1000 — 2026-09-10](unleashed-1000-2026-09-10.md) — optional 1,000-enemy/8× pressure mode; normal runs retain the 190 cap, while rendering, particles, damage text, projectiles, prop scans, and crowd separation receive density-safe budgets.
- [Swarm performance pass — 2026-09-12](swarm-performance-2026-09-12.md) — how to actually profile `stepWorld` (a Node script driving `createWorld`/`stepWorld` directly plus `node:inspector`'s CPU profiler, not guessing from source); `collideObstacles` and the crowd-separation pass were O(enemies × obstacles)/per-enemy-closure hotspots fixed with a per-frame obstacle spatial grid and an inlined neighbor loop; pickups had no cap and grew unbounded; `drawRig`/`drawShadow` now bake to an offscreen canvas once per (rig, anim, frame) and blit, the same technique `softBlob` already used for cloud gradients.
- [Sector Command design](sector-command-design.md) — the RTS/campaign mode across two passes: a Tier-1 "stolen army" economy and Tier-2 reinforcement beacons (which refill a squad but never inflate it past `squadCap`, and are mortal), why the friendly-fire exclusion and the capture-priming lethal floor both belong in `damageEnemy()` rather than in targeting, why mission success rides on `RunResult.missionComplete` instead of widening `RunOutcome` (surviving the clock used to bank every mission), why fog of war needed its own grid rather than the shadow caster and why it must never touch the simulation, the mobile rule that the movement stick and the RTS grammar never share a pointer-down, and that on touch a silent no-op is indistinguishable from a bug.
- [TCG / Lokpet crossover — 2026-09-13](tcg-lokpet-crossover.md) — a trading-card layer (`game/lok/types.ts`, `game/data/cards.ts`) adopts spend-ut-all's LOK Portable Asset Spec under namespace `g6.616-survivor`; card ownership is derived from existing `MetaState` fields, never separately persisted; 616 Survivor's own "LokPet" in-run mechanic is a same-named but unrelated system from spend-ut-all's "LokDex"; two new endless-mode `ChunkVariant`s (`scrapyard`, `overpass`) reuse existing obstacle kinds, and the file has *two* differently-typed "landmark" renderers that must not be confused.
- [LOK Universe Exchange](lok-universe-exchange.md) — cross-game *transfer* (export/import JSON), complementary to the TCG crossover above which is a read-only local card catalog: a kennel companion exports as flavor-only (no combat stats), an imported card becomes a separate `VisitingLokCard` that can never enter the kennel or a run, and every visiting card renders through a fixed local silhouette/palette, never data from the sender. `src/lib/lokCardExchange.ts` predates `game/lok/types.ts` from the TCG crossover pass and should be reconciled to import its manifest types from there instead of keeping a second hand-copied mirror of the same upstream contract.
- [Sixth Ward Cypher + LokPet Collectors — 2026-09-13](rap-cypher-lokpet-collectors-2026-09-13.md) — four rap-group legends plus a separately listed five-rank Collector class; character-driven +1/+2/+3/+5/+7 pet capacity, floor-pack and pack-pull bonuses, two-counter challenge progression, and the Card Credit Cipher Pack shop. The dated silhouette/palette/weapon table is a design lock and must not be silently redesigned.
