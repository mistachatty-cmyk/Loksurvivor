{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "packages/*",
    "scripts"
  ],
  "scripts": {
    "dev": "pnpm --filter @workspace/survivor-616 run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build && mkdir -p dist && cp -r artifacts/survivor-616/dist/public/* dist/",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "lint": "pnpm run typecheck"
  },
  "overrides": {
    "esbuild": "0.27.3"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "tsx": "^4.23.13",
    "typescript": "~5.9.3"
  }
}
